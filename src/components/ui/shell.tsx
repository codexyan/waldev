/**
 * Ukuran kerangka halaman publik.
 *
 * Satu lebar isi untuk seluruh situs (64rem), sama seperti acuannya. Kolom
 * yang lebih sempit daripada sebelumnya (dulu 80rem) membuat baris teks
 * berhenti di sekitar 90 karakter tanpa perlu `max-w-*` tambahan di tiap
 * paragraf, dan membuat judul terasa besar tanpa harus diperbesar.
 */
export const SHELL = "mx-auto w-full max-w-5xl px-6";

/** Jarak vertikal baku antar seksi. */
export const SECTION = "py-16 sm:py-24";
